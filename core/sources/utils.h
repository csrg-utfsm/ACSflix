#ifndef UTILS_H
#define UTILS_H

#include <string>
#include <sstream>

namespace bdt
{
    int str_to_int(std::string value);

    std::string size_to_str(size_t value);
}; // Bulk Data Transfer

#endif //UTILS_H
